1。由来
   来自Google的一篇文章    04年
2。要能清楚哪一行代码在edge（driver） node 上执行  哪一行在cluster（集群）执行

3。什么是mapreduce
    1。

4。什么是inputsplit（输入块）
    物理切分：128M；可能一个记录被切分两段
    逻辑切分：在物理分割符附近；会从下一个物理块中补充被切分的记录（下一个物理快的半条记录转移到上一个物理快然后做处理）
    一个mapper处理的是逻辑快（一个mapper处理一个逻辑快）
    mapper的个数开发人员无法决定。是有逻辑快的个数决定的
    逻辑块的个数无法改变。是有解码器确定的（inputformat接口）
